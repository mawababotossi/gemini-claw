{
    "name": "@geminiclaw/memory",
    "version": "0.1.0",
    "description": "Session persistence layer — JSON file store + JSONL transcripts",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "test": "vitest run"
    },
    "dependencies": {
        "lowdb": "^7.0.1",
        "zod": "^3.23.8"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.5.0",
        "vitest": "^2.0.0"
    }
}